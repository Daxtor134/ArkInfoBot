require('dotenv').config();

module.exports = {
      "name": "info",
      "description": "This command will give info about the person specified in the env. settings.",
      "args": true,
      "usage": "<platform [YouTube, Twitch]>",
      "guildOnly": true,
      execute(msg, args)
      {
          // TODO: Write info command.
      }
}